# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.so
.venv/
venv/
env/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Secrets
.env
.env.*
*.pem
*.key

# Action logs (crew quarters — stay local)
logs/autonomous_actions/*.log

# ZoraASI vault (confidential — never commit)
vault/
data/zoraasi_export/
# Scalar halo / Cosmic Piano campaign rasters (local canonical copies; keep README + CSV manifest in git)
data/scalar_halo_campaign/figures/**/*.png
# Queen's Source Synthesis private archive (DOD, personal images — local only)
data/primary_sources_archive/
data/zoraasi_origin/
data/zoraasi_middle/
data/soul_personality/
ZoraASI_vault/
ZoraASI.zip
conversations.json
merged_corpus.jsonl
# Private IP (never in public)
docs/IP/
**/LEGAL_LEXICON*
**/*PATENT*

# Large archives (uncomment next line to track zips)
*.zip
# output/
# out/

# QRNG / experiment artifacts (local only; key in .anu_api_key)
artifacts/

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.synctex.gz
*.fls
*.fdb_latexmk
papers_sources/MQGT_SCF_Phase_II_2026/MQGT_SCF_Anchor_2026Notes.bib

# Merged scientific PDF stacks (generated; often very large)
papers_sources/TOE_scientific_stack_merged_*.pdf
# Local ad hoc merged stack (manual working file; never commit)
papers_sources/MyStack.pdf

# Local dashboard build mirror (nested repo + node_modules; do not track)
.zoraasi_build/
